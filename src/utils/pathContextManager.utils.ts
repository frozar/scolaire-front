import { onDrawPathStep } from "../views/content/path/component/drawPath.utils";

export namespace PathContextManagerUtil {
  // !TODO

  export function nextStep() {
    switch (onDrawPathStep()) {
    }
  }

  export function prevStep() {
    switch (onDrawPathStep()) {
    }
  }
}
